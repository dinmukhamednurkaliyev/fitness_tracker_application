import 'package:flutter/material.dart';

class WeeklyListComponent extends StatelessWidget {
  const WeeklyListComponent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
