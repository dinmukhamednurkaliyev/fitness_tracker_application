name: "Pull Request: Labeler"

on:
  pull_request_target:
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write

jobs:
  run:
    name: "Run"
    runs-on: ubuntu-latest
    steps:
      - name: "Git: Checkout"
        uses: actions/checkout@v6.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          sparse-checkout: .github/configurations/pull_request_labels.yaml

      - name: "Labeler: Triage PR"
        uses: actions/labeler@v6.0.1
        with:
          repo-token: "${{ secrets.GITHUB_TOKEN }}"
          sync-labels: true
          configuration-path: .github/configurations/pull_request_labels.yaml
